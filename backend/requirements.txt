# backend/requirements.txt - Minimal Requirements for Optimized SAM Backend

# Core web framework
flask>=2.3.0
flask-cors>=4.0.0

# Scientific computing (minimal set)
numpy>=1.21.0
opencv-python>=4.5.0
pillow>=8.3.0

# Machine learning (core only)
scikit-learn>=1.0.0

# Graph processing for RAG merging
networkx>=2.6.0

# Optional: For actual SAM integration (development can work without these)
# torch>=1.9.0
# torchvision>=0.10.0
# segment-anything==1.0

# Development helpers
psutil>=5.8.0

# Note: This minimal set enables:
# - Mock SAM processing for development
# - Research-based semantic segmentation 
# - 8-12 region optimization
# - RAG-based region merging
# - Full frontend integration
#
# Total install time: ~2 minutes vs 20+ minutes with full SAM
# Total size: ~200MB vs 2GB+ with full SAM
#
# For production with real SAM:
# 1. Uncomment torch/torchvision/segment-anything
# 2. Download model checkpoints (2GB+)
# 3. Update app.py to use real SAM instead of mock